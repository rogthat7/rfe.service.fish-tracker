namespace RFE.Service.FT.Acceptance.Test.Shared.Models.Settings
{
    /// <summary>
    /// TestSettings are settings that can be injected via Environment Variable (meaning also via Teamcity)
    /// They should be used e.g. to set different passwords in other environments than DEV
    /// The property in the original Options class should be kept empty
    /// The implementation should happen then in the AcceptanceTestStartup class
    /// </summary>
    public class TestSettings
    {

        #region Properties       

        #endregion        
    }
}
